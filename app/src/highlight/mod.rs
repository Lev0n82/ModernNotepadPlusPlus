// Highlight module – placeholder for syntax highlighting using tree‑sitter or syntect

pub fn highlight(_text: &str) -> Vec<(usize, usize, String)> {
    // Returns a list of (start, end, style) tuples.
    // TODO: integrate tree‑sitter grammars and theme handling.
    Vec::new()
}
